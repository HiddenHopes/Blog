<% if @comment.errors.any? %>
alert('<%=comments errors.full_messeges.join('\n').html_safe %>')
$('#post-comments').append("<%= escape_javascript render 'comment', comment: @comment %>")
$("comment_body").val("");
<%end%>